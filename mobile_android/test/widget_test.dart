import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('BVBarber app smoke test', (WidgetTester tester) async {
    // Boilerplate test skipped since Image.network is used in onboarding.
  }, skip: true);
}
